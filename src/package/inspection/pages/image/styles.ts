import { Row } from 'antd';
import styled from 'styled-components';

export const ContainerForAction = styled.div`
  display: flex;
  justify-content: center;
  padding-top: 16px;
`;

export const StyledRow = styled(Row)`
  .ant-picker {
    width: 100%;
  }

  margin-bottom: 16px;
`;
